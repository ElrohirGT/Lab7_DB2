# All files related to doing the ETL process using code

To run the project remember to use nix develop and then run:

```bash
# On another terminal you'll need to start the Databases using docker compose...
pnpm start
```
